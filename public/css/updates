Things to do:
-Working on sidebar partial

Adding sidebar steps
- Define the route.  
router.get("/feed/:sport", ensureAuth, postsController.getSportFeed);
((the : allows dynamic filling of the paramater from url))

- Create controller function
    - Get sport from url (req.params.sport)
    - Format sport to match DB
    - Query DB for formattedSport
    - Render only the requested sport posts.
    - WORKING LETS GOOOOOOO

- Getting it to look nice.
    - Wrap sidebar and wrapper html together since they are going to sit side by side.
    
CURRENTLY FINISHED WITH SIDEBAR AND FEED LAYOUT\

- Working on comment functionality and display on postTest.ejs
Styling and content are set for the individual posts.  Working on getting comment functionality working.  Comments not saving to DB the way I have the model so check comments controller
-Comments model complete and saving to db.  
COMMENT SECTION WORKING AS INTENDED
- Comments weren't showing because I wasn't referencing the postID only the post.
---- const comments = await Comment.find({post: req.params.id}) would return no results because it is looking at the wrong field.  The field was called postID.


----PROFILE PAGE and utility function for date formatting
- Profile page setup to display the users bets.


old..UP NEXT: Utility function for date formatting.  Record tracking
- Add pagination to profile page to only pull up the most recent bets to not overload the system when the database gets larger over time.
-Record tracking: Allow an admin to have a thumb up and down when viewing bets for easy tracking.  Look into the deletion icon function and remix it?
- Figure out how to view a user's bets via a profile page of the user. Maybe copy  profile page, remove form, and populate using createdBy again?


COMPLETED: Viewing another user's profile.  Must compare the id with .toString for the comparison to work.  Has logic to redirect to profile or viewUserProfile page depending on the user chosen.
UP NEXT: 
On profile page, when clicking on a post, it gets an error.  Fix that. ----- FIXED

COMPLETED::
ADD FUNCTIONALITY FOR RECORD KEEPING
- Check likePost function and ejs.  Try to adjust post -> user instead to target user win/losses.  Add logic to check if a administrator is logged in to display those buttons.  

COMPLETED::
Added record tracking functionality with "addWin and addLoss" functions.
How it works - grab the postId from the (req.params.id).
             - populate the post with 'createdBy', 'userName' to reference the user that created the post
             - Find the user by id and update: (User.findByIdAndUpdate(post.createdBy, { $inc:{ losses: 1 } });)
             - Updates the wins/losses in DB and redirects back to the same post.

COMPLETED::
Adjust color of each prediction to display if it was a win or a loss
 - Add win/loss property to schema and createPost function
 - Update addWin controller to  update the "win" property.
 - Add if statement in ejs?
 COMPLETED ^^^^
 - Displays admin controls throughout application if admin account is logged in.
 - Updates the win/loss record in the DB
 - Updates the color of the prediction header based on the win/loss boolean.

UP NEXT::
Work on finding an API to stream the daily games available?
- Could display the games based on the sport
Find an API or list of teams for each sport to hardcode into the bet creation form?

(Current admin: AroundTheWeb 2j
 Test user: OfficialTestUser 2j )

COMPLETED::
Team dropdowns completed.
- Created a dictionary of teams by category.  Only did major USA teams for Hockey, NHL, MLB, and NFL currently
- Created a showTeams() function in public.js
- Function will grab the value from the sport dropdown and populate teams based on that.

COMPLETED::
- Displaying number of cards by screen size. 3, 2, or 1 at breakpoints 1300px and 1024px
- Sidebar filter functionality works to filter by sport the bet was made by.

NOT COMPLETED::
- Make sure the team dropdowns are added to the createdBet**** before saving to DB.
- Add a drowdown for ML, PL, RL etc depending on sport chosen to make the bets more consistent.
    - Decided to just add a placeholder for now.
- Dropdown for -110 -120 etc for consistency too?
- Could display the games based on the sport

FOUND FOR MLB:: Find an API or list of teams for each sport to hardcode into the bet creation form?
- Look for a way to get other sports as well.  Make one big fetch and save them each day?
- https://site.api.espn.com/apis/site/v2/sports/baseball/mlb/scoreboard?dates=YYYYMMDD
- Loading the games of the day with just their names in the getFeed() function.  
   - Look at how to display them.  Maybe in the header?
   - Check how to refresh the date to the next date

COMPLETED::
- Only allow a user to like a post once.  If they already have, it will unlike it and remove them from the array in the DB.
    - Used $push / $pull for the insert and removal of a user in the array on MONGODB.

WORKING ON:: 
- Profile picture for users
    - Add it after creation of user just to keep it easy by following the post image upload code.
- Allowing the "following" of users
    - Add an array to the user model to track the array of users they follow?

CURRENT PROJECT::
- Add "following" of user
    - Add follow button to user profile when visiting their link (Working)
    - Update user model to include who they follow(followedUsers) (Working)
    - Add route when the button is clicked (Working)
    - Add controller functionality (Working)
    - Make button turn from following to unfollow?
    - Remake profiles to include new user schema.